#!/usr/bin/env bash
# SSH key definitions: service → key type.
# Used by the git-ssh module to generate keys and build ~/.ssh/config.

declare -A SSH_KEYS=(
  [github]="ed25519"
  [bitbucket]="rsa"
  [pantheon]="rsa"
)
