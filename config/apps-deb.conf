#!/usr/bin/env bash
# DEB application categories.
# Values are space-separated app names; each app needs a matching installer
# script under the repository's expected installer path.

declare -A DEB_APP_CATEGORIES=(
  [dev-tools]="jetbrains-toolbox"
  [office]="libreoffice"
  [media]="zoom"
)
