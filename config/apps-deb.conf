#!/usr/bin/env bash
# DEB application categories.
# Values are space-separated app names; each app needs an installer script
# at tools/apps/<category>/<app>.sh

declare -A DEB_APP_CATEGORIES=(
  [dev-tools]="jetbrains-toolbox"
  [office]="libreoffice thunderbird"
  [media]="zoom"
)
